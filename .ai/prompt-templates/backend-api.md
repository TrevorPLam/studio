# Backend API Template

When creating a new API route in Next.js:

1. **Create route**: `app/api/[route]/route.ts`
2. **Use TypeScript**: Strict typing required
3. **Error handling**: Use ErrorBoundary and proper error responses
4. **Observability**: Use `lib/observability/` for metrics and tracing
